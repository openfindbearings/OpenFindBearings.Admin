# 认证集成方案

**版本：** v1.2.0
**日期：** 2026-09-11
**状态：** 与代码同步

---

## 变更记录

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.0.0 | 2026-06-08 | 初始设计：OpenIddict.AspNetCore 方案 |
| v1.1.0 | 2026-06-12 | 技术方案调整为标准 JWT Bearer，不引入 OpenIddict 客户端包 |
| v1.2.0 | 2026-09-11 | 代码审查对齐：① TenantConstants 修正为 `Realm = "openfindbearings"`（原 GUID 值不存在）；② JWT 配置改为从 Configuration 读取（非硬编码）；③ 移除 SaveTokens=true（Admin 手动管理 token）；④ 补充 SlidingExpiration；⑤ 用户管理 API 路径修正（toggle-status/unlock/permanent）；⑥ 角色控制器修正为 RoleController（非 RolesController）；⑦ 权限键数量修正为 17 个；⑧ 补充 merchant.view 权限键；⑨ 补充 unlock 端点 |

---

## 1. 概述

Admin 项目通过标准 JWT Bearer 认证接入 Identity 认证中心，实现用户登录、权限控制和本地 RBAC 管理。

### 1.1 认证架构

```
用户浏览器
    │
    ▼
Admin 项目（JWT Bearer + Cookie）
    │
    ├── 未登录 → 跳转 Identity /connect/authorize → 登录 → 回调拿 code → 换 JWT
    ├── 已登录 → JWT 验证通过 → 本地 RBAC 权限校验
    │
    ▼
API 项目（JWT 验证 + PermissionFilter）
    │
    └── 验证 JWT 有效性 → 校验具体权限
```

### 1.2 技术选型

| 组件 | 选型 | 说明 |
|------|------|------|
| 认证方案 | JWT Bearer + Cookie | 标准 JWT 验证，不用 OpenIddict 客户端包 |
| 认证中心 | Identity（OpenIddict） | OIDC 授权码流程 |
| 会话管理 | Cookie | 存储 JWT access_token |
| 权限控制 | 本地 RBAC（db_admin） | admin_role_permissions 表 |

## 2. OIDC 认证流程

### 2.1 授权码流程

```
1. 用户访问 Admin 任意页面
2. [Authorize] 未认证 → 401 → 跳转 /Account/Login
3. /Account/Login → 构造 OIDC 请求 → 跳转 Identity /connect/authorize
4. Identity 检查登录状态 → 未登录 → 登录页
5. 用户登录 → Identity 授予 code → 回调 Admin /Account/Callback?code=xxx
6. Admin 用 code + client_secret POST 到 Identity /connect/token
7. 拿到 access_token + refresh_token
8. access_token 存 Cookie → 跳转原始请求页面
```

### 2.2 Token 刷新（无感）

```
1. JWT 过期 → API 返回 401
2. BearerTokenHandler 通过 AdminTokenService 刷新
3. AdminTokenService 用 refresh_token POST 到 Identity /connect/token
4. 获取新 access_token + refresh_token
5. 更新 Cookie → 重试原始请求
6. refresh_token 也过期 → 跳转重新登录
```

### 2.3 登出流程

```
1. 用户点击退出
2. /Account/Logout → 清除本地 Cookie
3. 跳转 Identity /connect/logout
4. Identity 清除会话 → 回调 Admin /signout-callback-oidc
5. Admin 清理本地状态 → 跳转首页
```

## 3. 端口与回调配置

### 3.1 开发环境

| 项目 | 端口 | URL |
|------|------|-----|
| Identity | HTTPS 7201 | `https://localhost:7201` |
| Admin | HTTPS 7167 | `https://localhost:7167` |
| API | HTTPS 7183 | `https://localhost:7183` |

### 3.2 回调地址

| 配置项 | 值 |
|--------|-----|
| Admin CallbackPath | `/callback` |
| Identity redirect_uri | `https://localhost:7167/callback` |
| Identity PostLogoutRedirectUri | `https://localhost:7167/` |

### 3.3 生产环境回调地址维护

OpenIddict 的 redirect_uri 存储在数据库 `Applications` 表中。维护方式：

| 场景 | 操作 |
|------|------|
| 首次部署 | 更新 SeedData 或通过 ApplicationController 端点更新 |
| 域名变更 | 调用 `PUT /api/applications/{id}` 更新 redirect_uris |
| 多环境共存 | 同一 ClientId 可注册多个 redirect_uri |

## 4. NuGet 包

| 包名 | 用途 |
|------|------|
| `Microsoft.AspNetCore.Authentication.JwtBearer` | JWT Bearer 认证 |
| `Microsoft.AspNetCore.Authentication.Cookies` | Cookie 会话管理 |

**不使用** `OpenIddict.AspNetCore` — Admin 作为纯 JWT 客户端，通过 HTTP 调用 Identity 端点。

## 5. 认证中间件配置

```csharp
// Program.cs
builder.Services.AddAuthentication(CookieAuthenticationDefaults.AuthenticationScheme)
    .AddCookie(options =>
    {
        options.LoginPath = "/Account/Login";
        options.LogoutPath = "/Account/Logout";
        options.ExpireTimeSpan = TimeSpan.FromHours(8);
        options.SlidingExpiration = true;  // 滑动过期，活跃用户自动续期
    })
    .AddJwtBearer(options =>
    {
        // 从配置读取，不硬编码
        options.Authority = builder.Configuration["Identity:Authority"];
        options.Audience = builder.Configuration["Identity:ClientId"];
        options.RequireHttpsMetadata = false;
        // SaveTokens 不启用 — Admin 通过 AdminTokenService 手动管理 token 刷新和 Cookie 回写
    });
```

> 注：文档历史版本曾记载硬编码 `Authority = "https://localhost:7201"` 和 `SaveTokens = true`，实际代码从 Configuration 读取且未启用 SaveTokens。

## 6. 常量定义

```csharp
namespace OpenFindBearings.Admin.Constants;

public static class TenantConstants
{
    /// <summary>
    /// OpenFindBearings 租户 Realm 名称，与 Identity 种子数据一致
    /// </summary>
    public const string Realm = "openfindbearings";
}
```

> 注：文档历史版本曾记载 `OpenFindBearings = "00000000-0000-0000-0000-000000000001"`（GUID 值），实际代码中常量名为 `Realm`，值为 `"openfindbearings"` 字符串。

## 7. 用户管理（调 Identity API）

### 7.1 数据流

```
Admin UsersController
    │
    ├── GET /api/account/admin/users?tenantId=xxx → Identity 用户列表
    ├── POST /api/account/admin/users/{id}/toggle-status → Identity 启用/禁用
    ├── POST /api/account/admin/users/{id}/unlock → Identity 解锁账户
    ├── POST /api/account/admin/users/{id}/reset-password → Identity 重置密码
    ├── DELETE /api/account/admin/users/{id}/permanent → Identity 物理删除（仅已软删）
    ├── POST /api/account/admin/users/{id}/restore → Identity 恢复
    │
    └── 本地 db_admin
        ├── SELECT admin_user_roles WHERE UserId = @id → 获取角色
        ├── INSERT admin_user_roles → 分配角色
        ├── UPDATE admin_user_roles → 变更角色
        └── DELETE admin_user_roles WHERE UserId = @id → 清理角色
```

> 注：文档历史版本曾记载 `PATCH /api/Account/admin/users/{id}/status` 和 `DELETE /api/Account/admin/users/{id}`，实际代码使用 `POST .../toggle-status` 和 `DELETE .../permanent`。

### 7.2 页面功能

| 功能 | 说明 |
|------|------|
| 用户列表 | 搜索 + 分页 + 状态筛选 + 角色标记（已接入/未接入） |
| 重置密码 | 输入新密码 |
| 启用/禁用 | 切换用户状态（toggle-status） |
| 解锁 | 解除账户锁定（unlock） |
| 分配角色 | 选择 Admin 业务角色（admin/editor/viewer） |
| 删除/恢复 | 物理删除（仅已软删用户）+ 恢复 |

## 8. 角色管理（纯本地 RBAC）

### 8.1 数据存储

表：`admin_role_permissions`（db_admin）

| 字段 | 类型 | 说明 |
|------|------|------|
| Id | Guid | 主键 |
| RoleName | string | 角色名称 |
| PermissionKey | string | 权限键 |
| Granted | bool | 是否授予 |

### 8.2 页面功能

| 功能 | 说明 |
|------|------|
| 角色列表 | 按 RoleName 分组，显示权限数量 |
| 新建角色 | 输入角色名称 |
| 编辑角色 | 修改角色名称 |
| 删除角色 | 确认后删除（需清理 admin_user_roles 关联） |
| 分配权限 | 17 个 PermissionKey 勾选框 |

### 8.3 预置角色

| 角色 | 说明 | 权限数 |
|------|------|--------|
| admin | 超级管理员 | 17（全部） |
| editor | 业务员 | 10（含 system.view） |
| viewer | 审计员 | 6 |

## 9. 权限管理（纯本地，只读）

### 9.1 PermissionKey 枚举

共 17 个权限键（`PermissionKeys.All` 数组）：

| 模块 | 权限键 | 说明 |
|------|--------|------|
| 仪表盘 | `dashboard.view` | 查看仪表盘 |
| 轴承管理 | `bearing.view` | 查看轴承列表 |
| | `bearing.create` | 创建轴承 |
| | `bearing.edit` | 编辑轴承 |
| | `bearing.delete` | 删除轴承（软删除） |
| | `correction.review` | 纠错审核 |
| 商家管理 | `merchant.view` | 查看商家列表 |
| | `merchant.manage` | 商家 CRUD |
| | `merchant.verify` | 商家认证审核 |
| 同步管理 | `etl.manage` | ETL 任务管理 |
| 数据权限 | `data.restore` | 恢复已删除数据 |
| | `data.harddelete` | 彻底删除数据 |
| 系统管理 | `user.manage` | 用户管理 |
| | `role.manage` | 角色管理 |
| | `system.view` | 系统配置查看 |
| | `system.manage` | 系统配置管理 |
| | `audit.view` | 审计日志查看 |

> 注：文档历史版本曾记载 18 个或 21 个权限键，包含 `sync.trigger`/`sync.view`/`crawler.view`/`crawler.trigger`，这些权限键在 Admin 代码中不存在。实际 `PermissionKeys.All` 数组包含 17 个键。

### 9.2 页面功能

只读展示权限键表格，供管理员了解系统权限体系。

## 10. 侧边栏结构

```
认证管理
├── 用户列表    ← UsersController
├── 角色列表    ← RoleController
└── 权限列表    ← PermissionController
```

> 注：文档历史版本曾记载 `RolesController`，实际代码中控制器名为 `RoleController`。

## 11. 与 Identity 的边界

| 操作 | Admin 处理 | Identity 处理 |
|------|-----------|--------------|
| 用户认证 | Cookie 存储 JWT | OIDC 签发 token |
| 用户 CRUD | HTTP 调用 Identity API | 实际数据库操作 |
| 角色定义 | 本地 admin_role_permissions | 系统级 ASP.NET roles |
| 权限校验 | 本地 if/else | 不涉及 |
| 租户筛选 | 传 realm 参数 | 数据库过滤 |
